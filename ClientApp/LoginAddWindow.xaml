<Window x:Class="ClientApp.LoginAddWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientApp"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignDarkBackground}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="LoginAddWindow" MinHeight="350" Height="350" MinWidth="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="50"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition MaxHeight="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="3" Grid.ColumnSpan="2" BorderBrush="DarkGray" BorderThickness="2"></Border>
        <Border Grid.RowSpan="2" Grid.ColumnSpan="2" BorderBrush="DarkGray" BorderThickness="2"></Border>
        <Border Grid.Row ="0"  Grid.ColumnSpan="2" BorderBrush="DarkGray" BorderThickness="2"></Border>
        <Label Grid.Row="0" Grid.ColumnSpan="2" Foreground="AliceBlue" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Height="60">Adding new login</Label>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"></ColumnDefinition>
                <ColumnDefinition Width="9*"></ColumnDefinition>
                <ColumnDefinition Width="5*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition MaxHeight="40"></RowDefinition>
            </Grid.RowDefinitions>

            <Label Margin="10" Grid.Row="0"  Foreground="AliceBlue" FontSize="20" VerticalAlignment="Center">Name:</Label>
            <TextBox x:Name="a" Grid.Row="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Background="DarkGray" Margin="15"></TextBox>

            <Label Margin="10" VerticalAlignment="Center" Grid.Row="1"  Foreground="AliceBlue" FontSize="20">SavedLogin:</Label>
            <TextBox x:Name="b" Text="{Binding SavedLogin, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Background="DarkGray"  Margin="15"></TextBox>

            <Label Margin="10" VerticalAlignment="Center" Grid.Row="2"  Foreground="AliceBlue" FontSize="20">SavedPassword:</Label>
            <TextBox x:Name="c" Text="{Binding SavedPassword, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Background="DarkGray"  Margin="15"></TextBox>
            
            <Button Grid.Row="0" Grid.Column="3"  Click="ClearTextClick" Uid="1"  Content="Clear" HorizontalAlignment="Right"  FontSize="16"/>
            <Button Grid.Row="1" Grid.Column="3" Click="ClearTextClick" Uid="2"  Content="Clear" HorizontalAlignment="Right"     FontSize="16"/>
            <Button Grid.Row="2" Grid.Column="3" Click="ClearTextClick" Uid="2"  Content="Clear" HorizontalAlignment="Right"     FontSize="16"/>

            <Button Grid.Row="0" Grid.Column="3" Click="PasteTextToBox" Uid="1"  Content="Paste" HorizontalAlignment="Left"     FontSize="16"/>
            <Button Grid.Row="1" Grid.Column="3" Click="PasteTextToBox" Uid="2"  Content="Paste" HorizontalAlignment="Left"    FontSize="16"/>
            <Button Grid.Row="2" Grid.Column="3" Click="PasteTextToBox" Uid="2"  Content="Paste" HorizontalAlignment="Left"    FontSize="16"/>
            <CheckBox Grid.Row="3" Grid.Column="0" Foreground="AliceBlue" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center">IsFavourite</CheckBox>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"  Margin="10">Cancel</Button>
            <Button Grid.Column="1"  Margin="10" Click="AddAccountClick" >Add</Button>
        </Grid>


    </Grid>
</Window>